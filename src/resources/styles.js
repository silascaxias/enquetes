import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  defaultBorder: {
    borderRadius: 12,
    borderWidth: 1,
    borderColor: '#DA552F',
    backgroundColor: '#FFF',
  },
});
